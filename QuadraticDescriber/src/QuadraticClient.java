import java.util.Scanner;

/* This class is client code that uses the Quadratic Library(Quadratic library) to perform various mathematical task.
 * @author Alex Aguilan 
 * Version October 6th
 */
public class QuadraticClient {
	Scanner Console = new Scanner(System.in);
	// This ask for user input before progressing
	public static String quadDescriber (double a, double b, double c) {
		double fas = console.nextInt();
		double b = console.nextInt();
		double c = console.nextInt();
		System.out.println(Quadratic.quadForm(a, b, c));
	}
}
